db.product.find()
show collections;
db.show collections;
db.product.insertOne({"name": "patlican", "price": 25});
db.product.insertOne({"name": "patlican", "price": 25}});
db.insertone({"name": "patlican"});
db.insert({"name": "patlican"});
db.insert({"name": patlican});
use shop;
show dbs;
mongosh
for(var i = 0; i<100; i++){print(i);}
for(int i = 0; i<100; i++){}
print("zort");
clear
cd